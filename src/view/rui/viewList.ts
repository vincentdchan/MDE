import {ObservableCollection, ObservableObject} from "../../util/observable"

export interface ListViewProperties {
    source: ObservableCollection<any>;
}

export class ListView {

    constructor(props: ListViewProperties) {

    }

}
